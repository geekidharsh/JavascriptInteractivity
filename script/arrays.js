//Create an Array list

 var mylist = [20, 30, 40, 50, 12];
//function swaparray (item1, item2) {
//	var item3 = item2; //item3 now has vale of item 2
//	var item2 = item1; // item2 now has value of item1, so item2 is now item1
//	var item1 = item3; // item1 is now item3, which is equal to item2
//	return item2, item3;

//alert("the list of arrays is "+mylist);
//swaparray(mylist[0],mylist[1]);
//alert("the list of arrays after swapping is "+mylist);


/*alert("all the arrays in mylist are "+mylist);

/function logArrayEements(element, index, array){
	console.log("a["+ index + "]" +element);

}
*/


var myArrayName = ['Harsh', 'Pandey','Abhi','Raj','Sonam'];
console.log(myArrayName);


while(myArrayName.nextItem!=null){
console.log("current element is"+nextItem);
}